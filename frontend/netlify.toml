[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://smart-habit-garden-fresh.vercel.app"

# Redirect all routes to index.html for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
